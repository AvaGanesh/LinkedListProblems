{
  "name": "linked-list",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.json && node dist/index.js"
  },
  "author": "ganesh",
  "license": "ISC",
  "dependencies": {
    "common-js": "^0.3.8"
  }
}
